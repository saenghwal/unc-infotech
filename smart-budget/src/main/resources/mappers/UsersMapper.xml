<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.smartbudget.persistence.mappers.UsersMapper">

    <resultMap id="resultUser" type="io.smartbudget.persistence.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="createdAt" column="created_at"/>
        <result property="currency" column="currency"/>
    </resultMap>

    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into users (username, password)
        values (#{username}, #{password});
    </insert>

    <update id="merge" parameterType="User">
        update users
         <!--SET name = #{name}, currency = #{currency}-->
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="currency != null">
                currency = #{currency},
            </if>
        </set>
        where id = #{id};
    </update>

    <select id="findById" parameterType="java.lang.Long" resultMap="resultUser">
        select * from users where id = #{id};
    </select>

    <select id="findByUserName" parameterType="java.lang.String" resultMap="resultUser">
        select * from users where username = #{username};
    </select>
</mapper>